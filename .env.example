# Umgebungsvariablen für Produktion
# Kopiere diese Datei zu .env und fülle die Werte aus

# Server Port (optional, default: 3000)
PORT=3000

# Admin Passwort (WICHTIG: Ändere dies!)
ADMIN_PASSWORD=junggesellenabschied2025

# Node Environment
NODE_ENV=production

# Optional: Redis URL (für Skalierung)
# REDIS_URL=redis://localhost:6379

# Optional: Database URL (für persistente Speicherung)
# DATABASE_URL=postgresql://user:password@localhost:5432/logpose
